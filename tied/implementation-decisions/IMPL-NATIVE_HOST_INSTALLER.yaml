IMPL-NATIVE_HOST_INSTALLER:
  code_locations:
    files:
      - path: "native_host/install.sh"
        description: "POSIX installer"
      - path: "native_host/install.ps1"
        description: "Windows installer"
      - path: "src/features/storage/native-host-file-bookmark-adapter.js"
        description: "Adapter locus carrying full-block marker"
  cross_references:
    - "ARCH-NATIVE_HOST"
    - "REQ-NATIVE_HOST_WRAPPER"
  detail_file: "implementation-decisions/IMPL-NATIVE_HOST_INSTALLER.yaml"
  essence_pseudocode_path: "implementation-decisions/IMPL-NATIVE_HOST_INSTALLER-pseudocode.md"
  implementation_approach:
    details: []
    summary: "install.sh (macOS/Linux) and install.ps1 (Windows) parse args; copy binary and helper; write manifest; register host path with browser."
  metadata:
    created:
      author: "MCP converter"
      date: "2026-02-18"
    last_updated:
      author: "AI Agent"
      date: "2026-07-29"
      reason: "Full-block three-way sync"
    last_validated:
      date: "2026-02-18"
      result: "pass"
      validator: "MCP converter"
  name: "Native Host Installer Implementation"
  rationale:
    benefits: []
    problems_solved: []
    why: "Install scripts copy wrapper binary and helper(s) to fixed install dir and register native messaging host with Chrome/Chromium via generated manifest (and on Windows, registry)."
  related_decisions:
    composed_with:
      - "IMPL-NATIVE_HOST_WRAPPER"
    depends_on: []
    see_also:
      - "IMPL-NATIVE_HOST_WRAPPER"
    supersedes: []
  status: "Active"
  traceability:
    architecture:
      - "ARCH-NATIVE_HOST"
    code_annotations:
      - "ARCH-NATIVE_HOST"
      - "IMPL-NATIVE_HOST_INSTALLER"
      - "REQ-NATIVE_HOST_WRAPPER"
    requirements:
      - "REQ-NATIVE_HOST_WRAPPER"
    test_coverage_note: "No unit tests; coverage via E2E or manual. Reason: native host installer and platform setup."
    tests:
      - "tests/unit/native-host-installer.test.js"
