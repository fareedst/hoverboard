IMPL-LOCAL_BOOKMARKS_INDEX_IMPORT:
  code_locations:
    files:
      - path: "src/ui/bookmarks-table/bookmarks-table.html"
        description: "Import fieldset (file input, mode radios, Import to, Import button last, #import-result)"
      - path: "src/ui/bookmarks-table/bookmarks-table.js"
        description: "runImport, pending/final import-result, parseImportFile"
      - path: "src/ui/bookmarks-table/bookmarks-table-csv.js"
        description: "parseCsv for import"
      - path: "src/ui/bookmarks-table/bookmarks-table.css"
        description: "import-result is-pending / is-final colors"
  cross_references:
    - "ARCH-LOCAL_BOOKMARKS_INDEX_IMPORT"
    - "REQ-LOCAL_BOOKMARKS_INDEX_IMPORT"
  detail_file: "implementation-decisions/IMPL-LOCAL_BOOKMARKS_INDEX_IMPORT.yaml"
  essence_pseudocode_path: "implementation-decisions/IMPL-LOCAL_BOOKMARKS_INDEX_IMPORT-pseudocode.md"
  implementation_approach:
    details:
      - "Browser source loads and collapses chrome.bookmarks records, filters by title/URL/folder, and selects by cleaned URL"
      - "Browser source uses Skip/Overwrite/Merge tags, root-stripped folder tags, extra tags, and target-specific existing rows"
      - "File source keeps Only new/Overwrite and Local|File|Sync|Browser targets"
      - "Index URL source=browser selects the live Browser source and Import footer; legacy Browser Import launchers use this URL"
    summary: "Index Import supports File CSV/JSON and a live Browser source with shared saveBookmark orchestration."
  metadata:
    created:
      author: "AI Agent"
      date: "2026-02-18"
    last_updated:
      author: "AI Agent"
      date: "2026-07-29"
      reason: "Corpus close-loop: Import to includes Browser"
    last_validated:
      date: "2026-02-18"
      result: "pass"
      validator: "AI Agent"
  name: "Local Bookmarks Index Import Implementation"
  rationale:
    benefits:
      - "Hidden file input + Import button; Only new / Overwrite existing; Import to Local | File | Sync | Browser; parseCsv and JSON; saveBookmark per row; result message"
    problems_solved:
      - "No import UI or parsing for index page"
    why: "Implement import UX and parse/save flow using existing saveBookmark and CSV/JSON format; Import to includes Browser (Index import; distinct from Browser Bookmark Import page which excludes Browser as target)."
  related_decisions:
    composed_with:
      - "IMPL-BOOKMARK_ROUTER"
      - "IMPL-LOCAL_BOOKMARKS_INDEX"
      - "IMPL-LOCAL_BOOKMARKS_INDEX_EXPORT"
    depends_on:
      - "IMPL-LOCAL_BOOKMARKS_INDEX"
    see_also:
      - "IMPL-LOCAL_BOOKMARKS_INDEX_EXPORT"
    supersedes: []
  status: "Active"
  traceability:
    architecture:
      - "ARCH-LOCAL_BOOKMARKS_INDEX_IMPORT"
    code_annotations:
      - "IMPL-LOCAL_BOOKMARKS_INDEX_IMPORT"
    requirements:
      - "REQ-LOCAL_BOOKMARKS_INDEX_IMPORT"
    tests:
      - "tests/unit/bookmarks-table-import-status.test.js"
      - "tests/unit/bookmarks-table-import.test.js"
