IMPL-CONTEXT_MENU_QUICK_ACCESS:
  code_locations:
    files:
      - path: "src/core/service-worker.js"
        description: "setupContextMenus() creates parent and four children; contextMenus.onClicked.addListener dispatches to same four actions"
    functions: []
  cross_references:
    - "ARCH-QUICK_ACCESS_ENTRY"
    - "REQ-QUICK_ACCESS_ENTRY"
  essence_pseudocode_path: "implementation-decisions/IMPL-CONTEXT_MENU_QUICK_ACCESS-pseudocode.md"
  implementation_approach:
    details:
      - "onClicked: switch(info.menuItemId); same logic as handleCommand (sidePanel.open with _sidePanelWindowId, openOptionsPage, OPEN_BOOKMARKS_INDEX_TAB for index, tabs.create for import). Use browser.contextMenus (or chrome.contextMenus) for cross-browser; guard for undefined."
      - "SW setupContextMenus (or init): create parent Hoverboard and four items; onClicked switch on menuItemId and perform same actions as handleCommand."
    summary: "Parent Hoverboard with four items; hoverboard-open-bookmarks-index uses OPEN_BOOKMARKS_INDEX_TAB (create + REQUEST_SIDE_PANEL_CLOSE)."
  metadata:
    created:
      author: "AI Agent"
      date: "2026-02-26"
    last_updated:
      author: "AI Agent"
      date: "2026-07-29"
      reason: "Context menu index open uses OPEN_BOOKMARKS_INDEX_TAB"
  name: "Context Menu Quick Access"
  rationale:
    benefits:
      - "Parent \"Hoverboard\" with four items; onClicked performs same actions as command handler (sidePanel.open, openOptionsPage, tabs.create x2)"
    problems_solved:
      - "No context menu entry for side panel, options, bookmarks index, import"
    why: "Implements REQ-QUICK_ACCESS_ENTRY and ARCH-QUICK_ACCESS_ENTRY by providing right-click menu entry to the same four targets."
  related_decisions:
    depends_on:
      - "ARCH-QUICK_ACCESS_ENTRY"
      - "REQ-QUICK_ACCESS_ENTRY"
    see_also:
      - "IMPL-EXTENSION_COMMANDS"
      - "IMPL-LOCAL_BOOKMARKS_INDEX"
    supersedes: []
  status: "Active"
  traceability:
    architecture:
      - "ARCH-QUICK_ACCESS_ENTRY"
    code_annotations:
      - "ARCH-QUICK_ACCESS_ENTRY"
      - "IMPL-CONTEXT_MENU_QUICK_ACCESS"
      - "REQ-QUICK_ACCESS_ENTRY"
    requirements:
      - "REQ-QUICK_ACCESS_ENTRY"
    tests:
      - "tests/unit/service-worker-quick-access.test.js"
