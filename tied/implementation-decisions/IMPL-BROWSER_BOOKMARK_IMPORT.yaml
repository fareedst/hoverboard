IMPL-BROWSER_BOOKMARK_IMPORT:
  code_locations:
    files:
      - path: "src/ui/bookmarks-table/bookmarks-table.html"
        description: "Index Import source selector, live Browser filters, selection table, conflict controls, tags, target, and result"
      - path: "src/ui/bookmarks-table/bookmarks-table.js"
        description: "Index live Browser source load/filter/render/import orchestration"
      - path: "src/ui/bookmarks-table/bookmarks-table-browser-import.js"
        description: "Pure Browser-tree normalization, duplicate collapse, folder filtering, root-stripped tags, payload, and target conflict helpers"
      - path: "src/ui/browser-bookmark-import/browser-bookmark-import-utils.js"
        description: "Shared tree flattening, tag sanitization, root stripping, and duplicate URL helpers"
      - path: "src/ui/browser-bookmark-import/browser-bookmark-import.html"
        description: "Legacy compatibility redirect to bookmarks-table.html?source=browser"
  cross_references:
    - "ARCH-BROWSER_BOOKMARK_IMPORT"
    - "REQ-BROWSER_BOOKMARK_IMPORT"
  detail_file: "implementation-decisions/IMPL-BROWSER_BOOKMARK_IMPORT.yaml"
  essence_pseudocode_path: "implementation-decisions/IMPL-BROWSER_BOOKMARK_IMPORT-pseudocode.md"
  implementation_approach:
    details:
      - "Browser source builds folder tags with stripRoots=true, merges extra tags, and preserves earliest dateAdded as time"
      - "Browser source filters by title, URL, or folder path and by folder path, with select-all and per-record selection"
      - "Browser source loads chrome.bookmarks.getTree directly in the Index page and collapses duplicate cleaned URLs before rendering"
      - "Existing Browser rows are excluded from target conflict lookup by filtering aggregate rows to the selected Local|File|Sync backend"
      - "File CSV/JSON import remains unchanged and may still target Browser"
    summary: "Integrate live Browser-tree import into Local Bookmarks Index; preserve legacy entry URL as source=browser redirect."
  metadata:
    created:
      author: "AI Agent"
      date: "2026-02-18"
    last_updated:
      author: "AI Agent"
      date: "2026-08-03"
      reason: "Synchronize live Browser import blocks and coverage"
    last_validated:
      date: "2026-02-18"
      result: "pass"
      validator: "AI Agent"
  name: "Browser Bookmark Import Implementation"
  rationale:
    benefits:
      - "getTree + flattenTree; conflict Skip/Overwrite/Merge; folder+extra tags; Import to Local|File|Sync (Browser excluded); saveBookmark per row"
    problems_solved:
      - "No UI for importing browser bookmarks into Hoverboard-owned backends"
    why: "The live browser-tree source is still valuable for selective migration and folder-to-tag conversion, but it belongs beside file import in the storage-aware Index rather than on a duplicate page."
  related_decisions:
    composed_with:
      - "IMPL-BOOKMARK_ROUTER"
      - "IMPL-LOCAL_BOOKMARKS_INDEX"
      - "IMPL-LOCAL_BOOKMARKS_INDEX_IMPORT"
    depends_on: []
    see_also:
      - "IMPL-LOCAL_BOOKMARKS_INDEX"
      - "IMPL-LOCAL_BOOKMARKS_INDEX_IMPORT"
    supersedes: []
  status: "Active"
  traceability:
    architecture:
      - "ARCH-BROWSER_BOOKMARK_IMPORT"
    code_annotations:
      - "IMPL-BROWSER_BOOKMARK_IMPORT"
    requirements:
      - "REQ-BROWSER_BOOKMARK_IMPORT"
    tests:
      - "tests/integration/tools-toolbar-launchers-composition.integration.test.js"
      - "tests/playwright/extension-bookmarks-index-controls.spec.js"
      - "tests/playwright/extension-lower.spec.js"
      - "tests/unit/bookmarks-table-browser-import.test.js"
