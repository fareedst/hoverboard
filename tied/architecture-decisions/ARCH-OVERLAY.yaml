ARCH-OVERLAY:
  alternatives_considered:
    - name: "Modal dialogs"
      cons:
        - "Intrusive"
      pros:
        - "Simpler"
      rejected_reason: "Breaks flow"
  cross_references:
    - "ARCH-OVERLAY_CONTROLS"
    - "REQ-OVERLAY_AUTO_SHOW_CONTROL"
    - "REQ-OVERLAY_SYSTEM"
  decision: "Parent architecture for the in-page hover overlay system (show/hide, auto-show, transparency, content). Control layout geometry is owned by child ARCH-OVERLAY_CONTROLS; testability by ARCH-OVERLAY_TESTABILITY. REQ-OVERLAY_SYSTEM and REQ-OVERLAY_AUTO_SHOW_CONTROL primary here; layout/refresh REQs primary on ARCH-OVERLAY_CONTROLS."
  implementation_approach:
    details:
      - "Configurable auto-show on page load"
      - "Control layout: see ARCH-OVERLAY_CONTROLS (Close 8px/8px; Refresh 8px/40px; 24px min touch)"
      - "Hover-based activation and transparency"
    summary: "Semi-transparent overlay with hover activation and auto-show; delegates fixed control positions to ARCH-OVERLAY_CONTROLS."
  metadata:
    created:
      author: "AI Agent"
      date: "2026-07-28"
    last_updated:
      author: "AI Agent"
      date: "2026-07-29"
      reason: "ARCH corpus Phase 6: overlay parent; defer layout to controls"
  name: "Overlay System Architecture"
  rationale:
    benefits:
      - "No page disruption"
      - "Seamless experience"
    problems_solved:
      - "Context switching"
      - "Intrusive bookmark dialogs"
    why: "Non-intrusive bookmarking interface"
  related_decisions:
    depends_on:
      - "REQ-OVERLAY_AUTO_SHOW_CONTROL"
      - "REQ-OVERLAY_SYSTEM"
    informs:
      - "IMPL-OVERLAY"
    see_also:
      - "ARCH-OVERLAY_CONTROLS"
      - "ARCH-OVERLAY_TESTABILITY"
  status: "Active"
  traceability:
    code_annotations:
      - "ARCH-OVERLAY"
    implementation:
      - "IMPL-OVERLAY"
    requirements:
      - "REQ-OVERLAY_AUTO_SHOW_CONTROL"
      - "REQ-OVERLAY_SYSTEM"
    tests: []
