ARCH-LOCAL_BOOKMARKS_INDEX_EXPORT:
  allbookmarks: "Full list from getAggregatedBookmarksForIndex; items include storage field (local|file|sync|browser); used for Export all."
  alternatives_considered: []
  cross-references: "REQ-LOCAL_BOOKMARKS_INDEX_EXPORT, IMPL-LOCAL_BOOKMARKS_INDEX_EXPORT"
  cross_references:
    - "IMPL-LOCAL_BOOKMARKS_INDEX_EXPORT"
    - "REQ-LOCAL_BOOKMARKS_INDEX_EXPORT"
  csv: "Header row (Title, URL, Tags, Time, Storage, Shared, To read, Notes). Storage column Local|File|Sync|Browser."
  decision: "Export is implemented entirely on the index page (client-side) from in-memory allBookmarks or filteredBookmarks. Items include storage field local|file|sync|browser. CSV Storage column Local|File|Sync|Browser. Export all covers the aggregate index scope (local+file+sync+browser). No new background message; Blob download via temporary anchor."
  download_mechanism:
    - "Build string (CSV or JSON)."
    - "Create Blob (e.g. `text/csv; charset=utf-8` or `application/json`)."
    - "Create object URL; create `<a download=\"filename\">`, set href to object URL; programmatic click(); revokeObjectURL; remove anchor."
  filteredbookmarks: "Result of applying search and filters (and current sort); used for \"Export displayed\"."
  implementation_approach:
    details:
      - "Blob + object URL + temporary download anchor"
      - "buildCsv emits Storage column Local|File|Sync|Browser"
      - "Export all uses aggregate index list (local+file+sync+browser)"
      - "Export displayed uses filteredBookmarks"
    summary: "Client-side export from allBookmarks/filteredBookmarks; CSV/JSON; Storage local|file|sync|browser."
  json: "(optional): Array of bookmark objects as returned from the index (description, url, tags, time, shared, toread, extended, etc.)."
  metadata:
    created:
      author: "MCP converter"
      date: "2026-02-18"
    last_updated:
      author: "AI Agent"
      date: "2026-07-29"
      reason: "ARCH corpus Phase 1: export Storage includes sync and browser"
    last_validated:
      date: "2026-02-18"
      result: "pass"
      validator: "MCP converter"
  name: "Local Bookmarks Index Export Architecture"
  rationale:
    benefits: []
    problems_solved: []
    why: "See detail file"
  related:
    - "ARCH-LOCAL_BOOKMARKS_INDEX (index page this export extends)"
    - "IMPL-LOCAL_BOOKMARKS_INDEX_EXPORT (export UI and exportBookmarks implementation)"
  related_decisions:
    depends_on:
      - "ARCH-LOCAL_BOOKMARKS_INDEX"
    informs:
      - "IMPL-LOCAL_BOOKMARKS_INDEX_EXPORT"
    see_also:
      - "ARCH-STORAGE_INDEX_AND_ROUTER"
  selected_scope: "\"Export selected\" uses bookmarks whose URL is in the current selection (selectedUrls); list built from allBookmarks.filter(b => selectedUrls.has(b.url)). Export and Move UI are placed below the table; index page header is compact (minimal height)."
  status: "Active"
  summary: "Client-side CSV/JSON export from index page; Storage values local|file|sync|browser; Export all = aggregate index scope."
  traceability:
    code_annotations:
      - "ARCH-LOCAL_BOOKMARKS_INDEX_EXPORT"
    implementation:
      - "IMPL-LOCAL_BOOKMARKS_INDEX_EXPORT"
    requirements:
      - "REQ-LOCAL_BOOKMARKS_INDEX_EXPORT"
    tests:
      - "tests/unit/bookmarks-table-export.test.js"
