ARCH-STORAGE:
  alternatives_considered:
    - name: "All local storage"
      cons:
        - "No cross-device sync"
      pros:
        - "Simpler"
      rejected_reason: "Users need settings sync"
    - name: "External storage"
      cons:
        - "Privacy concerns"
      pros:
        - "More capacity"
      rejected_reason: "Privacy is core value"
  cross_references:
    - "REQ-CHROME_STORAGE_USAGE"
    - "REQ-CONFIG_PORTABILITY"
  decision: "ARCH-STORAGE covers chrome.storage for extension settings and config portability only (sync for settings, local for temp/caches, export/import of config). It does not define bookmark storage backends — those are ARCH-STORAGE_INDEX_AND_ROUTER and the five providers."
  implementation_approach:
    details:
      - "Export/import for config portability"
      - "Local storage for temporary data"
      - "Sync storage for user settings"
    summary: "chrome.storage.sync for settings, local for temp data, export/import workflows"
  metadata:
    created:
      author: "AI Agent"
      date: "2026-07-28"
    last_updated:
      author: "AI Agent"
      date: "2026-07-29"
      reason: "ARCH corpus Phase 5: clarify settings vs bookmark backends"
  name: "Chrome Storage Settings Strategy"
  rationale:
    benefits:
      - "Cross-device settings"
      - "Data portability"
    problems_solved:
      - "Privacy violations"
      - "Settings lost across devices"
    why: "Cross-device sync and privacy preservation"
  related_decisions:
    depends_on:
      - "REQ-CHROME_STORAGE_USAGE"
      - "REQ-CONFIG_PORTABILITY"
    informs:
      - "IMPL-STORAGE"
    see_also:
      - "ARCH-CONFIG_STRUCTURE"
      - "ARCH-STORAGE_INDEX_AND_ROUTER"
  status: "Active"
  traceability:
    code_annotations:
      - "ARCH-STORAGE"
    implementation:
      - "IMPL-STORAGE"
    requirements:
      - "REQ-CHROME_STORAGE_USAGE"
      - "REQ-CONFIG_PORTABILITY"
    tests: []
