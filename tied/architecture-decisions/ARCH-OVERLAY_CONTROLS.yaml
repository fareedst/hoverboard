ARCH-OVERLAY_CONTROLS:
  alternatives_considered:
    - name: "Dynamic positioning"
      cons:
        - "Risk of overlaps"
      pros: []
      rejected_reason: "Complex pages cause issues"
    - name: "CSS-only"
      cons:
        - "Host styles override"
      pros: []
      rejected_reason: "Need JS enforcement"
  cross_references:
    - "REQ-OVERLAY_CONTROL_LAYOUT"
    - "REQ-OVERLAY_REFRESH_ACTION"
  decision: "Canonical overlay control layout: Close at top 8px left 8px; Refresh at top 8px left 40px (32px horizontal gap from Close); minimum 24px touch targets; fixed absolute positioning with inline styles and theme CSS variables; full ARIA. Child of ARCH-OVERLAY; primary owner of REQ-OVERLAY_CONTROL_LAYOUT and REQ-OVERLAY_REFRESH_ACTION."
  implementation_approach:
    details:
      - "24px minimum touch targets"
      - "Close at top: 8px; left: 8px"
      - "Full ARIA metadata; theme CSS variables"
      - "Refresh at top: 8px; left: 40px (32px gap)"
    summary: "Fixed absolute positioning; Close 8/8; Refresh 8/40; 24px min touch; ARIA; theme variables."
  metadata:
    created:
      author: "AI Agent"
      date: "2026-07-28"
    last_updated:
      author: "AI Agent"
      date: "2026-07-29"
      reason: "ARCH corpus Phase 6: resolve spacing contradiction; own layout REQs"
  name: "Overlay Control Layout Architecture"
  rationale:
    benefits:
      - "Consistent layout"
      - "Full accessibility"
    problems_solved:
      - "Accessibility issues"
      - "Control overlaps"
    why: "Predictable, accessible control placement"
  related_decisions:
    depends_on:
      - "ARCH-OVERLAY"
    informs:
      - "IMPL-OVERLAY_CONTROLS"
    see_also:
      - "ARCH-OVERLAY_TESTABILITY"
  status: "Active"
  traceability:
    code_annotations:
      - "ARCH-OVERLAY_CONTROLS"
    implementation:
      - "IMPL-OVERLAY_CONTROLS"
    requirements:
      - "REQ-OVERLAY_CONTROL_LAYOUT"
      - "REQ-OVERLAY_REFRESH_ACTION"
    tests:
      - "Overlay close button positioning tests"
      - "Overlay refresh button tests"
