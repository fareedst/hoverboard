# [ARCH-STRUCTURED_LOGGING] Structured logging with context and levels
# Migrated from [IMPL-LOGGER_CONTEXT_LEVELS] [ARCH-STRUCTURED_LOGGING] [REQ-STRUCTURED_LOGGING] (was LOG-001)..003
ARCH-STRUCTURED_LOGGING:
  cross_references:
    - "REQ-STRUCTURED_LOGGING"
  implementation_approach:
    details:
      - "[IMPL-LOG_LEVEL_CONFIG] [ARCH-STRUCTURED_LOGGING] [REQ-STRUCTURED_LOGGING] (was LOG-002) → IMPL-LOG_LEVEL_CONFIG (getLogLevel from process.env.NODE_ENV)"
      - "[IMPL-LOGGER_CONTEXT_LEVELS] [ARCH-STRUCTURED_LOGGING] [REQ-STRUCTURED_LOGGING] (was LOG-001) → IMPL-LOGGER_CONTEXT_LEVELS (context, levels, formatMessage, debug/info/warn/error)"
      - "[IMPL-LOGGER_LEGACY] [ARCH-STRUCTURED_LOGGING] [REQ-STRUCTURED_LOGGING] (was LOG-003) → IMPL-LOGGER_LEGACY (log(), noisy export)"
    summary: "src/shared/logger.js — Logger class with context, getLogLevel(), shouldLog(), formatMessage(), debug/info/warn/error; createLogger(context); legacy log(context, ...args) and noisy"
  metadata:
    created:
      author: "AI Agent"
      date: "2026-02-17"
    last_updated:
      author: "AI Agent"
      date: "2026-02-17"
      reason: "Numbered token reinterpretation"
  name: "Structured Logging"
  rationale:
    benefits:
      - "Clear log attribution and level control"
    problems_solved:
      - "Inconsistent console usage and no component identification"
    why: "Context-based logging with level filtering; environment-aware (debug in dev, warn+ in production); legacy log() during migration"
  related_decisions:
    depends_on:
      - "REQ-STRUCTURED_LOGGING"
    informs:
      - "IMPL-LOG_LEVEL_CONFIG"
      - "IMPL-LOGGER_CONTEXT_LEVELS"
      - "IMPL-LOGGER_LEGACY"
    see_also: []
  status: "Active"
  traceability:
    code_annotations:
      - "ARCH-STRUCTURED_LOGGING"
    implementation:
      - "IMPL-LOG_LEVEL_CONFIG"
      - "IMPL-LOGGER_CONTEXT_LEVELS"
      - "IMPL-LOGGER_LEGACY"
    requirements:
      - "REQ-STRUCTURED_LOGGING"
