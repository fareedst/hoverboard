ARCH-LOCAL_BOOKMARKS_INDEX_IMPORT:
  alternatives_considered:
    - name: "New bulk-import message"
      cons: "Adds backend API; per-row saveBookmark is sufficient for typical use"
      pros: "Single round-trip for large files"
      rejected_reason: "Deferred; can add later if needed"
  cross-references: "REQ-LOCAL_BOOKMARKS_INDEX_IMPORT, IMPL-LOCAL_BOOKMARKS_INDEX_IMPORT"
  cross_references:
    - "REQ-LOCAL_BOOKMARKS_INDEX_IMPORT"
  decision: "Keep one Import control group in the Local Bookmarks Index with two sources: rich CSV/JSON files and the live chrome.bookmarks tree. File import may target Local|File|Sync|Browser; live Browser source import targets Local|File|Sync only. Live source conflicts are resolved against the selected target backend, never against Browser source rows."
  detail_file: "architecture-decisions/ARCH-LOCAL_BOOKMARKS_INDEX_IMPORT.yaml"
  implementation_approach:
    details:
      - "Keep Browser excluded only for live Browser-source destinations; retain Browser for file import"
      - "Reuse saveBookmark for both sources; preserve file record fields and map Browser folder paths to root-stripped tags"
      - "Use a source selector to switch between file controls and live Browser-tree controls"
    summary: "Single Index Import group with File and live Browser sources."
  metadata:
    created:
      author: "AI Agent"
      date: "2026-02-18"
    last_updated:
      author: "AI Agent"
      date: "2026-07-29"
      reason: "Close-out: restore Index Import Browser target (exclusion only on Browser Bookmark Import page)"
    last_validated:
      date: "2026-02-18"
      result: "pass"
      validator: "AI Agent"
  name: "Local Bookmarks Index Import Architecture"
  rationale:
    benefits:
      - "Clear distinction from Browser Bookmark Import page"
      - "Import into all Hoverboard router backends including Store B"
      - "Reuses saveBookmark path"
    problems_solved:
      - "Need to load bookmarks into Local/File/Sync/Browser from files"
    why: "Bulk import into router-backed stores including Browser; Browser Bookmark Import page keeps Browser excluded as target"
  related:
    - "ARCH-LOCAL_BOOKMARKS_INDEX (index page this import extends)"
    - "ARCH-LOCAL_BOOKMARKS_INDEX_EXPORT (same CSV/JSON format as export)"
    - "IMPL-LOCAL_BOOKMARKS_INDEX_IMPORT (import UI and parse + save loop)"
  related_decisions:
    depends_on:
      - "REQ-LOCAL_BOOKMARKS_INDEX_IMPORT"
    informs:
      - "IMPL-LOCAL_BOOKMARKS_INDEX_IMPORT"
    see_also:
      - "ARCH-LOCAL_BOOKMARKS_INDEX"
      - "ARCH-LOCAL_BOOKMARKS_INDEX_EXPORT"
  status: "Active"
  summary: "Import is client-side in a dedicated Import control group: parse CSV/JSON (export format), send each record via saveBookmark with preferredBackend Local|File|Sync|Browser; only-new skips URLs in allBookmarks; overwrite sends all. Pending then final result in #import-result. No new backend message. Distinct from Browser Bookmark Import page (Local|File|Sync only)."
  traceability:
    code_annotations:
      - "ARCH-LOCAL_BOOKMARKS_INDEX_IMPORT"
    implementation:
      - "IMPL-LOCAL_BOOKMARKS_INDEX_IMPORT"
    requirements:
      - "REQ-LOCAL_BOOKMARKS_INDEX_IMPORT"
    tests:
      - "tests/unit/bookmarks-table-import-status.test.js"
      - "tests/unit/bookmarks-table-import.test.js"
