REQ-STORAGE_MODE_DEFAULT:
  category: "Functional"
  configmanager: "`getDefaultConfiguration().storageMode === 'local'`; `getStorageMode()` / `setStorageMode(mode)`."
  cross-references: "ARCH-LOCAL_STORAGE_PROVIDER, ARCH-STORAGE_INDEX_AND_ROUTER, IMPL-LOCAL_BOOKMARK_SERVICE, IMPL-BOOKMARK_ROUTER, IMPL-MOVE_BOOKMARK_UI"
  detail_file: "requirements/REQ-STORAGE_MODE_DEFAULT.yaml"
  how_validated:
    - "Default configuration includes storageMode: local."
    - "getStorageMode() returns local when no stored override, pinboard/file/sync/browser when stored, and pinboard for invalid stored value (fallback)."
    - "setStorageMode accepts local|pinboard|file|sync|browser; invalid mode throws."
    - "Unit tests in tests/unit/config-manager.test.js including browser mode."
  metadata:
    created:
      author: "MCP converter"
      date: "2026-02-18"
    last_updated:
      author: "AI Agent"
      date: "2026-07-29"
      reason: "LEAP: Options Storage Mode includes Browser"
    last_validated:
      date: "2026-02-18"
      result: "pass"
      validator: "MCP converter"
  name: "Storage Mode Default and Dual-Use"
  options_page: "Storage Mode section with radios Local Storage (offline), Pinboard (cloud), File, Sync, and Browser (native Chrome bookmarks); selection is persisted and triggers provider swap via SWITCH_STORAGE_MODE message."
  priority: "P1"
  purpose:
    - "Make the dual-use case explicit: local-first, Pinboard optional."
    - "Preserve optional Pinboard integration for users who want it."
    - "Reduce friction for new users: no signup or API required by default."
  rationale:
    benefits: []
    problems_solved: []
    why: "See detail file"
  related:
    - "ARCH-LOCAL_STORAGE_PROVIDER (strategy and default choice)"
    - "IMPL-LOCAL_BOOKMARK_SERVICE (local provider implementation)"
    - "REQ-CHROME_STORAGE_USAGE (storage API usage)"
  related_requirements:
    depends_on: []
    related_to:
      - "REQ-BROWSER_BOOKMARK_STORAGE"
    supersedes: []
  satisfaction_criteria:
    - criterion: "Default config has storageMode local"
    - criterion: "Options page allows switching among Local, Pinboard, File, Sync, and Browser storage modes"
    - criterion: "Service worker uses active provider from config; no external service required for default path"
    - criterion: "When popup shows a storage backend as selected, saving a bookmark uses that backend (save follows the highlight)"
  status: "Implemented"
  summary: "The extension supports bookmark storage modes local, pinboard, file, sync, and browser. The default is local so most users need no account. Users may switch modes in Options (Storage Mode) and per-bookmark via Save to."
  traceability:
    architecture:
      - "ARCH-BROWSER_BOOKMARK_PROVIDER"
      - "ARCH-LOCAL_STORAGE_PROVIDER"
      - "ARCH-STORAGE_INDEX_AND_ROUTER"
    code_annotations:
      - "REQ-STORAGE_MODE_DEFAULT"
    implementation:
      - "IMPL-BOOKMARK_ROUTER"
      - "IMPL-BROWSER_BOOKMARK_SERVICE"
      - "IMPL-LOCAL_BOOKMARK_SERVICE"
      - "IMPL-MOVE_BOOKMARK_UI"
    tests:
      - "tests/unit/config-manager.test.js"
  validation_criteria:
    - method: "Manual verification"
      coverage: "See detail file"
