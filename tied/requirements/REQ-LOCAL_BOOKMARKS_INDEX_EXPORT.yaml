REQ-LOCAL_BOOKMARKS_INDEX_EXPORT:
  at_least_one_format: "CSV (recommended for table-like export; columns include Title, URL, Tags, Time, **Storage**, Shared, To read, Notes); optionally JSON for full record structure."
  category: "Functional"
  cross-references: "ARCH-LOCAL_BOOKMARKS_INDEX_EXPORT, IMPL-LOCAL_BOOKMARKS_INDEX_EXPORT, REQ-LOCAL_BOOKMARKS_INDEX"
  detail_file: "requirements/REQ-LOCAL_BOOKMARKS_INDEX_EXPORT.yaml"
  download: "is triggered client-side (no backend export message)."
  export_all: "Downloads a file containing every bookmark shown in the index (local + file; same as full list loaded on the page)."
  export_control(s): "in the index page UI below the bookmarks table (with Move selected to); clear scope (all, displayed, selected)."
  export_displayed: "Downloads a file containing only the rows currently shown (after search/filter/sort)."
  export_selected: "Downloads a file containing only the currently selected bookmarks (checkbox selection). Button enabled only when at least one bookmark is selected."
  how_validated:
    - "Manual: Open index page, click \"Export all\" and \"Export displayed\"; verify CSV downloads with correct row counts and content; verify \"Export displayed\" reflects current filters/sort."
    - "Manual: Select one or more bookmarks; verify \"Export selected\" is enabled and downloads CSV with only those bookmarks; verify disabled when selection is cleared."
  metadata:
    created:
      author: "MCP converter"
      date: "2026-02-18"
    last_updated:
      author: "MCP converter"
      date: "2026-02-18"
      reason: "Converted from monolithic"
    last_validated:
      date: "2026-02-18"
      result: "pass"
      validator: "MCP converter"
  name: "Local Bookmarks Index Export"
  priority: "P1"
  purpose:
    - "Allow users to backup or port index data outside the extension."
    - "Support exporting the full local+file set, the current filtered/sorted view, or only selected bookmarks, including storage location."
  rationale:
    benefits: []
    problems_solved: []
    why: "See detail file"
  related:
    - "ARCH-LOCAL_BOOKMARKS_INDEX_EXPORT (client-side export architecture)"
    - "IMPL-LOCAL_BOOKMARKS_INDEX_EXPORT (export UI and serialization)"
    - "REQ-LOCAL_BOOKMARKS_INDEX (index page this export extends)"
  related_requirements:
    depends_on: []
    related_to: []
    supersedes: []
  satisfaction_criteria:
    - criterion: "See detail file"
  status: "Implemented"
  summary: "From the local bookmarks index page, the user can **export bookmarks as a downloadable file**. Three scopes are supported: **all** (entire list), **displayed** (filtered/sorted rows), **selected** (checkbox-selected rows; button enabled when at least one selected). Export and Move controls are placed **below the bookmarks table**. The file is downloaded client-side (e.g. CSV or JSON). CSV includes a **Storage** column (Local | File | Sync)."
  traceability:
    architecture:
      - "ARCH-LOCAL_BOOKMARKS_INDEX_EXPORT"
    code_annotations:
      - "REQ-LOCAL_BOOKMARKS_INDEX_EXPORT"
    implementation:
      - "IMPL-LOCAL_BOOKMARKS_INDEX_EXPORT"
    tests:
      - "tests/unit/bookmarks-table-export.test.js"
  validation_criteria:
    - method: "Manual verification"
      coverage: "See detail file"
