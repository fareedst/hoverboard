REQ-MOVE_BOOKMARK_STORAGE_UI:
  category: "Functional"
  cross_references:
    - "ARCH-MOVE_BOOKMARK_UI"
    - "IMPL-MOVE_BOOKMARK_UI"
    - "REQ-PER_BOOKMARK_STORAGE_BACKEND"
  metadata:
    created:
      author: "MCP converter"
      date: "2026-02-18"
    last_updated:
      author: "AI Agent"
      date: "2026-07-29"
      reason: "Corpus close-loop: moves via Save to only; drop dedicated Local/File toggle criterion"
    last_validated:
      date: "2026-02-18"
      result: "pass"
      validator: "MCP converter"
  name: "Move Bookmark Storage UI"
  priority: "P1"
  rationale:
    benefits: []
    problems_solved: []
    why: "See detail file"
  related_requirements:
    depends_on: []
    related_to: []
    supersedes: []
  satisfaction_criteria:
    - criterion: "Local↔File↔Browser (and other backend) moves use the same Save to buttons; there is no separate dedicated Local/File toggle control."
    - criterion: "Popup Save to shows current bookmark storage and select-one buttons for Pinboard, Local, File, Sync, and Browser; click moves via moveBookmarkToStorage when a bookmark exists."
    - criterion: "Save follows the highlight: creating or updating a bookmark from the popup uses the currently selected (highlighted) storage backend; popup sends preferredBackend and router honors it (REQ-STORAGE_MODE_DEFAULT, IMPL-BOOKMARK_ROUTER, IMPL-MOVE_BOOKMARK_UI)."
  status: "Implemented"
  summary: "The user can see the current storage of the current page bookmark (Pinboard, Local, File, Sync, or Browser) and move it to another storage backend via Save to buttons in the popup. Move is implemented as copy to target backend, delete from source, and update storage index. Options page explains default storage for new bookmarks with a five-way choice (Local | Pinboard | File | Sync | Browser). Index Actions also support Move selected to (Local | File | Sync | Browser)."
  traceability:
    architecture:
      - "ARCH-MOVE_BOOKMARK_UI"
      - "ARCH-STORAGE_INDEX_AND_ROUTER"
    code_annotations:
      - "REQ-MOVE_BOOKMARK_STORAGE_UI"
    implementation:
      - "IMPL-BOOKMARK_ROUTER"
      - "IMPL-MOVE_BOOKMARK_UI"
    tests:
      - "tests/integration/message-handler-router-storage.integration.test.js (move/aggregate with preferredBackend)"
      - "tests/unit/bookmark-router.test.js (moveBookmarkToStorage including browser)"
  validation_criteria:
    - method: "Manual verification"
      coverage: "See detail file"
